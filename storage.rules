rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Dosyaları yalnız Cloud Functions Admin SDK yönetir. İstemci erişimi kapalıdır.
      allow read, write: if false;
    }
  }
}
